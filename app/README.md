# App (Next.js)

App directory for the customer/portal UI. Useful commands:
- `npm install`
- `npm run dev`
- `npm run build`

Deployment (Vercel) root directory for this project should be `app/`. Configure `API_BASE_URL`/`NEXT_PUBLIC_API_URL` to point to the deployed API project.
