# API (Express)

Express API for appointments, customers, branding, and auth.

Useful commands:
- `npm install`
- `npm start`

Deployment (Vercel) root directory for this project should be `api/`. Ensure required environment variables (Supabase keys, API_BASE_URL, etc.) are set in this project.